import palette from "./palette.json";
/**
 * Shared mobile design colors.
 * Change values in palette.json and both React Native code + NativeWind classes pick them up.
 */
export const Colors = palette;
